{
    "name": "deploy",
    "dependencies": {
        "@sap/hdi-deploy": "^4",
        "@sap/cds": "^8.5.0"
    },
    "engines": {
        "node": "^20.0.0"
    },
    "scripts": {
        "start": "node node_modules/@sap/hdi-deploy/deploy.js",
        "build": "npm i && npx cds build .. --for hana --production"
    }
}
