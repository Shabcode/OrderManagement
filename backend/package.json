{
    "name": "backend-cds",
    "version": "1.0.0",
    "description": "Generated by cds-services-archetype",
    "license": "ISC",
    "scripts": {
        "store-hdi-credentials": "cds deploy --to hana:ordermanagement-db --store-credentials",
        "get-vcap-services": "cds bind --exec -- node -e 'console.log(process.env.VCAP_SERVICES)'",
        "local-with-hana": "cds bind --exec -- mvn spring-boot:run -Dspring-boot.run.profiles=cloud"
    },
    "devDependencies": {
        "@sap/cds-dk": "^8.5.0"
    },
    "cds": {
        "features": {
            "assert_integrity": "db"
        }
    }
}
